# API URL 설정
NEXT_PUBLIC_API_URL=http://localhost:3001

# 데이터베이스 설정
DB_HOST=monsilserver.iptime.org
DB_PORT=3306
DB_NAME=blog_db
DB_USER=min
DB_PASSWORD=f8tgw3lshms!

# NextAuth 설정 (사용하지 않지만 호환성을 위해 유지)
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your-secret-key

# 개발 환경에서 데이터베이스 연결 건너뛰기 (false로 변경)
SKIP_DATABASE_CONNECTION=false

# Google AdSense (선택사항)
NEXT_PUBLIC_ADSENSE_PUBLISHER_ID=ca-pub-4467822003966307

# MCP 토큰 (로컬 테스트용)
MCP_TOKEN=test-token
